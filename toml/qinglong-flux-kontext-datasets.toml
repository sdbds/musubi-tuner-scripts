# resolution, caption_extension, batch_size, enable_bucket, bucket_no_upscale must be set in either general or datasets

# general configurations
[general]
resolution = [1024,1024]
caption_extension = ".txt"
batch_size = 1
enable_bucket = true
bucket_no_upscale = false
num_repeats = 4

[[datasets]]
image_directory = "./train/image/figure/figure"
control_directory = "./train/image/figure/anime"
cache_directory = "./train/image/figure/figure/cache"
flux_kontext_no_resize_control = false

# other datasets can be added here. each dataset can have different configurations